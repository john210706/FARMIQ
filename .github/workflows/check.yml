name: FarmIQ checks
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: farmiq-test
          POSTGRES_DB: farmiq_test
        ports: ['5432:5432']
        options: --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:farmiq-test@127.0.0.1:5432/farmiq_test
      DIRECT_URL: postgresql://postgres:farmiq-test@127.0.0.1:5432/farmiq_test
      TEST_DATABASE_URL: postgresql://postgres:farmiq-test@127.0.0.1:5432/farmiq_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run db:generate
      - run: npm run db:migrate -w farmiq-backend
      - run: npm run check
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
